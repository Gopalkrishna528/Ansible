-name: Demo on a variable
Hosts: all
vars: 
URL: play.google.com
tasks: 
  -name: print a variable
    ansible.builtin.debug 
      msg: "URL = {{URL}}"
    vars:
      URL: task.google.com
    
    - name: Demo on Inv variables
  hosts: all
  tasks:
    - name: Print a Variable
      ansible.builtin.debug:
        msg: "URL = {{URL}}"
        
        - name: Demo on File variables
  hosts: all
  vars_files:
    - vars.yml
  tasks:
    - name: Print a Variable
      ansible.builtin.debug:
        msg: "URL = {{URL}}"
      
      
    
